@model IEnumerable<NewProject.Models.Test>

@{
    ViewBag.Title = "Search";
}

@using (Html.BeginForm("Search", "Home", FormMethod.Post))
{
    //the following are search options
    <b> Search Option: </b>
    @Html.RadioButton("option", "Name") <div>Name</div>  
    @Html.RadioButton("option", "Password") <div> Password </div>
    @Html.RadioButton("option", "Accountname")<div>Accountname</div> 
    @Html.TextBox("search") <input type = "submit"
    name = "submit"
    value = "Search" />
    }
<h2>Search</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Accountname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Password)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Phoneno)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Accountname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Password)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Phoneno)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
