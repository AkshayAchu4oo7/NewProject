@model IEnumerable<NewProject.Models.Test>



@using (Html.BeginForm("Tables", "Home", FormMethod.Post))
{
    <div class="form-group">
        <div class="col-md-10">
            @Html.TextBox("search")
        </div>


        <input type="submit"
               name="submit"
               value="Search" class="btn btn-default" />
        <br />
    </div>
}
<h2>Tables</h2>

@*<div class="test">

    @foreach (var item in ViewBag.Accountname)
    {
        <div class="test1">
            <h2>@item.Id</h2>
            <div>@item.Name</div>
            <div>@item.Accountname</div>
            <div>@item.Phoneno</div>
            <div>@item.Password</div>
            @Html.ActionLink("One data", "Trys", "Home", new { Id = item.Id, Name = item.Name, name1= item.Accountname, name2=item.Phoneno, name3=item.Password }, null)
        </div>
    }
</div>*@
<div class="test">
    @foreach (var item in Model)
    {
        <div class="test1">
            <h1>  @Html.DisplayFor(modelItem => item.Name)</h1>

            <h3>
                @Html.DisplayFor(modelItem => item.Accountname)
            </h3>

            <h3>
                @Html.DisplayFor(modelItem => item.Password)
            </h3>
            <h3>
                @Html.DisplayFor(modelItem => item.Phoneno)
            </h3>
         <h5>
             @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
             @Html.ActionLink("Details", "Details", new { id = item.Id }) |
             @Html.ActionLink("Delete", "Delete", new { id = item.Id })

         </h5>
            @Html.ActionLink("One data", "Trys", "Home", new { Id = item.Id, Name = item.Name, name1 = item.Accountname, name2 = item.Phoneno, name3 = item.Password }, null)


        </div>
    }
</div>
<style>
    .mytablesearch{
        width:30px;
        height:15px;
        background-color:antiquewhite;
        color:black;
    }
    .test1 {
        width: auto;
        height: auto;
        color: black;
    
      
        border-width: 4px;
        border: groove;
        cursor: pointer;
        font-family: Verdana,sans-serif;
        font-size: 15px;
        line-height: 1.5;
    }

        .test1:hover {
            color: white;
            background-color: black;
        }

    .test {
        display: grid;
        background-image: url('../../Content/coffeehouse2.jpg');
       
        grid-template-columns: 1fr 1fr 1fr;
        grid-row-align: center;
        grid-gap: 6.2vw 0.4rem;
        color: red;
        width: auto;
        height: auto;
        opacity: 0.8;
    }
</style>
